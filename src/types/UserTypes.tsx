export interface UserType {
    id: number;
    first_name: string;
    last_name: string;
    school_email: string;
    email: string;
    roles: string[];
    role_ids: number[];
    profile_picture_url: string;
    is_active: boolean;
}
